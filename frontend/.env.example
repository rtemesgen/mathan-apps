VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_DEMO_EMAIL=demo@mathan.local
VITE_DEMO_PASSWORD=mathan2026

# Set VITE_STANDALONE=true only for a fully local build.
# Mobile releases use false so signed-in users can sync while guests remain local-only.
VITE_STANDALONE=false
